# GOAL-2 h=20 Stage 0b-α: KILLED by its own frozen provenance clause — recorded as the finding

STATUS: finding record. Executes Stage 0b-α of the MERGED design
(`doc/design/2026-08-24-goal2-h20-redesign.md`) and reports that its kill
criterion fires on leg1. No discretion is exercised; the clause is applied
verbatim. GOAL-2 parks on the live shadow panel's accrual clock (§4).

## 1. The frozen criterion being applied

From the merged design (verbatim): "**KILL (0b-α): any leg's recipe
provenance cannot demonstrate a 2024–2026-only selection window**; or feature
coverage < 80% on either the training or the evaluation window." The design's
premise was: "Recipe selection consumed 2024–2026 outcomes only; nothing in
2016–2023 was ever an input to any leg's development [provenance recorded per
leg in 0b-α]." 0b-α's job was to verify that premise per leg. It is false for
leg1.

## 2. Per-leg provenance (the 0b-α record)

The production blend has exactly two legs [VERIFIED — pinned
`configs/strategy_config.json`, `ranking.panel_scoring.components`]:

**leg0 — panel LTR (`rank:pairwise` xgb, alpha158_fund), content pin
`sha256:6461b827…`.** Selection instrument = the WF gate over the committed
retrain corpus: 39 retrains, cutoffs **2024-01-01 → 2026-03-09**, lookahead
60d [VERIFIED — `walkforward_manifest.json`, first/last entries read
2026-08-24]. Selection-outcome window ≈ 2024-01..2026-05. The 06-23
rejection studies that left this recipe unchanged (trendscan, fundmom,
neutralization) were evaluated on the same corpus cuts [VERIFIED —
`2026-06-23-trendscan-wf-gate.csv` is per-cut on the WF corpus]. Leg0's
provenance therefore DOES demonstrate a 2024–2026-only selection window,
with one caveat recorded honestly: rejection decisions are also selection,
and at least one rejected alternative was screened on longer windows (see
leg1's chain) — the surviving recipe is conditioned on those readings in the
weak sense of "kept as-is."

**leg1 — slow momentum residual v0 (12-1, window=252/skip=21,
ledger-served, `momentum-v0-fd65161a20b29314`).** Its selection chain, in
the record's own words:

1. Signal hunt 2026-06-28: evaluated canonical momentum on an **8-year
   panel 2018-05-30 → 2026-06-26**; the pivotal reading (mom_12_1's h=20
   edge clears on 2021–2026 but collapses at 8y) is a statement ABOUT
   2018–2020 outcomes [VERIFIED — scan record; the 5y-vs-8y collapse is
   the documented lesson].
2. C3 residual momentum 2026-07-02: the residual-momentum measurement that
   motivated this leg family ran on the committed OHLCV parquets over the
   **full 2017–2026 window** (142-name fixed universe) [VERIFIED —
   `2026-07-02-c3-residual-momentum.md` §7, read 2026-08-24].
3. GOAL-7 preregs (2026-08-01, 2026-08-05) and GOAL-8 S1 blend membership
   (2026-08-04) inherit that chain.

Leg1's provenance does not merely FAIL to demonstrate a 2024–2026-only
selection window — it demonstrates the opposite: **2017–2023 outcomes,
including the design's entire proposed evaluation window (2020–2023) and
most of its training window (2016–2019), were direct inputs to the studies
that selected this leg.**

## 3. Verdict

**KILL at 0b-α.** The design evaluates the ASSEMBLED blend (both arms carry
all legs), so one contaminated leg contaminates the assembled test; the
clause fires on leg1 regardless of leg0's cleaner record. Feature-coverage
measurement (the second 0b-α check) is moot and was not run — a panel that
cannot be evaluated honestly does not need its coverage measured.

What would resurrect a historical window: a leg family whose entire
development provably never touched pre-2024 outcomes. No such leg exists —
the momentum family was chosen BECAUSE of how momentum behaved in
2017–2023. This is not fixable by shifting windows again: every shift so
far (h=60 → h=20 → pre-corpus quarantine) has died on a contamination the
previous round did not see, which is itself evidence that the historical
route is exhausted.

## 4. The honest continuation (already in the merged design, now the ONLY line)

The merged design names the promotion path: "re-fit on the live legs'
accruing shadow panel, whose first labeled multi-leg rows land ~2026-09 and
whose own ESS clock applies." With the legs frozen 2026-08-04 and scores
accruing daily, the arithmetic is fixed ex ante:

- ESS bar: n_eff ≥ 12 non-overlapping h=20 blocks (gap ≥ 20 trading days)
  on the ASSEMBLED labeled panel — 12 × 20 = **240 trading days** of
  labeled rows.
- First scored rows 2026-08-04; labels trail by 20 trading days; the panel
  reaches the bar ≈ 240 trading days later: **~2027-07** [DERIVED — trading
  calendar arithmetic from the frozen constants; checkpoint dates may move
  a few weeks with holidays/outages].
- Until then the only permitted computation on the accruing panel is the
  n_eff count itself (reported, never adjudicated) — same discipline as the
  MoE C-clock.

**GOAL-2 state: PARKED on the accrual clock.** No conditional-weight fit,
no transformer, no 10-minute upgrade before the assembled panel clears its
own bar. The h=60 kill (#1031), the h=20 redesign (#1045), and this record
together close every historical route; the estimand stays alive strictly on
data the legs could not have seen.
