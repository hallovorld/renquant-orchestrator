# GOAL-2v3 Stage I-0 — substrate + ESS census: KILL gate PASS

Date: 2026-08-27 (evening). Design: orch#1072 (MERGED; the kill bar,
estimator, horizon, and eligibility rule were frozen there BEFORE this
census ran).

## Execution

- Fetch: Alpaca IEX 10-min bars, development window only (2020-08-01..
  2024-06-30), candidate seed = watchlist ∪ r2k dev names ∪ fundamentals
  universe = 2,144 names → **2,124 with bars, 20 empty**, 739MB, $0,
  ~3h at ≤190 req/min. Resumable fetcher committed.
- Census per the frozen declarations, implemented literally (r2 after
  review): bar-level ICs at every feasible bar-time t=13..25 (score
  s₀ = −trailing-13-bar return, label = following-13-bar return); a BLOCK
  is the mean of one session's 13 bar-time ICs — length h in bar time, and
  the close-truncated times 26..38 (dropped by the frozen rule) form the
  ≥h gap between consecutive blocks. Drift rule in its two declared layers
  and order: >1% name-days excluded from ALL downstream use (retained
  names included), then names breaching on >5% of ELIGIBLE days excluded
  entirely — **99 names excluded**. Trailing
  eligibility as frozen; regime = K5 approx-regime on daily SPY, pure
  upsample (intraday vol overlay reserved for I-1). ρ̂₁ reported RAW
  alongside the floored value the conservative formula uses; the full
  block-mean series, eligibility counts, per-name breach rates, and
  sha256 of every consumed bar file live in the committed audit artifact.

## The table (full JSON committed alongside)

| regime | n_blocks | episodes | ρ̂₁ raw→used | **n_eff_adj** | s₀ block IC |
|---|---|---|---|---|---|
| **BEAR** | 191 | 11 | −0.051 (raw) → 0.00 used | **191** | +0.031 |
| BULL_CALM | 520 | 16 | +0.003 (used as-is) | 516.4 | +0.020 |
| BULL_VOLATILE | 159 | 16 | −0.096 (raw) → 0.00 used | 159 | +0.016 |
| CHOPPY | 47 | 10 | −0.119 (raw) → 0.00 used | 47 | +0.048 |

917 block sessions; median eligible cross-section 1256 names.

## Verdict

**KILL gate (BEAR n_eff_adj ≥ 30 @ h=13): PASS at 191 — 6.4× the bar.**
Raw dependence estimates are small and mostly NEGATIVE (BEAR −0.051, VOL
−0.096, CHOPPY −0.119, CALM +0.003) — consecutive block-mean ICs carry no
material positive dependence, so the conservative floor changes almost
nothing (CALM 520→516.4) and the granularity thesis holds on honest
numbers: intraday blocks mint usable evidence. Every regime clears 30, so
Stage I-1 conditioning is fundable in ALL states.

Bonus observation (diagnostic, not a gate): the naive frozen baseline s₀
(13-bar reversal) carries positive IC in every regime — the substrate has
cross-sectional structure before any model exists.

## Next (per the merged design)

Stage I-1: per-state base models on 10-min features, forward-chaining OOF
inside the development window; life screen block-t ≥ 1.0 at h=13 on
dependence-adjusted units, each base's own ρ̂₁ re-estimated; no transformer
and no meta-learner before a base passes. Evaluation window (2024-07..
2026-06) remains sealed.
