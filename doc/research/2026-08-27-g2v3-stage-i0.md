# GOAL-2v3 Stage I-0 — substrate + ESS census: KILL gate PASS

Date: 2026-08-27 (evening). Design: orch#1072 (MERGED; the kill bar,
estimator, horizon, and eligibility rule were frozen there BEFORE this
census ran).

## Execution

- Fetch: Alpaca IEX 10-min bars, development window only (2020-08-01..
  2024-06-30), candidate seed = watchlist ∪ r2k dev names ∪ fundamentals
  universe = 2,144 names → **2,124 with bars, 20 empty**, 739MB, $0,
  ~3h at ≤190 req/min. Resumable fetcher committed.
- Census per the frozen declarations (in the committed script's docstring):
  RTH bars only; trailing eligibility (≥60 sessions history, ≥80%
  trailing-60 coverage); drift rule |IEX close/official close − 1| > 1%
  name-day exclusion, >5% breach rate name exclusion — **432 names
  drift-excluded** (the IEX thin-feed caveat is real and the rule caught
  it); regime = K5 approx-regime on daily SPY, pure upsample (the intraday
  vol overlay is reserved for I-1, per the frozen doc); s₀ = −(trailing
  13-bar return); one IC observation per session at h=13 with within-session
  labels; AR(1)-adjusted ESS with fail-closed (<8 pairs) estimator.

## The table (full JSON committed alongside)

| regime | n_blocks | episodes | pairs | ρ̂₁ | **n_eff_adj** | s₀ mean IC |
|---|---|---|---|---|---|---|
| **BEAR** | 191 | 11 | 180 | 0.00 | **191** | +0.036 |
| BULL_CALM | 521 | 16 | 505 | 0.00 | 521 | +0.017 |
| BULL_VOLATILE | 159 | 16 | 143 | 0.00 | 159 | +0.017 |
| CHOPPY | 47 | 10 | 37 | 0.00 | 47 | +0.063 |

918 IC sessions; median eligible cross-section 1,256 names.

## Verdict

**KILL gate (BEAR n_eff_adj ≥ 30 @ h=13): PASS at 191 — 6.4× the bar, and
~96× the daily line's 2 BEAR blocks.** The measured ρ̂₁ = 0 (floored;
episode-internal block-mean ICs are uncorrelated across sessions) — the
granularity thesis holds: intraday blocks mint usable evidence rather than
re-counting the same regime episode. Every regime clears 30, so Stage I-1
conditioning is fundable in ALL states, not only BEAR.

Bonus observation (diagnostic, not a gate): the naive frozen baseline s₀
(13-bar reversal) carries positive IC in every regime — the substrate has
cross-sectional structure before any model exists.

## Next (per the merged design)

Stage I-1: per-state base models on 10-min features, forward-chaining OOF
inside the development window; life screen block-t ≥ 1.0 at h=13 on
dependence-adjusted units, each base's own ρ̂₁ re-estimated; no transformer
and no meta-learner before a base passes. Evaluation window (2024-07..
2026-06) remains sealed.
