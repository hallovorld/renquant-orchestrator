# Extension-corpus builder patch (orch#948, review P1)
#
# Applies to: RenQuant/scripts/build_alpha158_qlib.py
# Source file sha256: 3b666826e705642d26e2eb4c0f7983e6d3b96915519dd003711a04965f968c8f
# Apply to a SCRATCH COPY only — never the umbrella tree. The REPO_ROOT
# hunk pins the read-only source tree for an out-of-tree copy (substitute
# your local RenQuant path); the dropna hunk is the substantive change.
# Invocation: python <scratch_copy>.py --output <scratch>/alpha158_extension_full.parquet
#
--- /Users/renhao/git/github/RenQuant/scripts/build_alpha158_qlib.py	2026-06-01 10:04:36
+++ /private/tmp/claude-502/-Users-renhao-git-github-renquant-orchestrator/428feb92-8ee7-4b4f-afed-1e4fa82ef367/scratchpad/build_alpha158_ext.py	2026-08-09 19:44:49
@@ -94,7 +94,7 @@
 import numpy as np
 import pandas as pd
 
-REPO_ROOT = Path(__file__).resolve().parent.parent
+REPO_ROOT = Path("/Users/renhao/git/github/RenQuant")  # scratch copy: pin the source tree (read-only inputs)
 
 logging.basicConfig(
     level=logging.INFO,
@@ -445,7 +445,12 @@
     panel["split_label"] = panel["date"].map(date_split)
     # Dates beyond existing dataset → assign to test (most recent data)
     panel["split_label"] = panel["split_label"].fillna("test")
-    panel = panel.dropna(subset=["fwd_5d_excess", "fwd_20d_excess", "fwd_60d_excess"])
+    # EXTENSION BUILD (orch#939 scratch variant): keep label-NaN tail rows --
+    # the extension window needs FEATURE rows for scoring; labels realize on
+    # their own clock. Only rows missing ALL features are dropped.
+    n_lab = panel[["fwd_5d_excess", "fwd_20d_excess", "fwd_60d_excess"]].notna().all(axis=1).sum()
+    log.info("extension variant: keeping %d label-NaN tail rows (labeled: %d)",
+             len(panel) - n_lab, n_lab)
     log.info("After split: %d rows, %d tickers", len(panel), panel["ticker"].nunique())
 
     train_mask = panel["split_label"] == "train"
