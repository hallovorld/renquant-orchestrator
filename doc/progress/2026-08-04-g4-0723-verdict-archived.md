# 2026-08-04 — the stranded 2026-07-23 G4 verdict, archived verbatim

STATUS:    archival commit; the document is added BYTE-UNTOUCHED
WHAT:      doc/research/2026-07-23-g4-ensemble-AUTHORITATIVE.md + its
           evidence dir (two result JSONs + the runner script, 16K),
           found 2026-08-04 during worktree hygiene sitting UNTRACKED and
           identical in two long-dead worktrees (wt-cutindep, wt-lanepre)
           — written 07-23, 3-way cross-audited, never committed.
WHY/DIR:   Research history must not live only in abandoned worktrees.
           The document is committed verbatim (its integrity claim is
           "3-way cross-audited"; editing it would break that), with the
           archival context HERE instead:
           - Its disposition ("KILL G4 on current evidence, bounded
             reopening path") is about the XGB + PatchTST two-expert
             ensemble as of 07-23, single-split powered screen.
           - Subsequent record it must be read against: the #569 KILL
             claim was independently re-derived as WEAKENED (checkpoint
             mis-attribution) and reverted by #570; the 08-01 double-
             audited premise re-assessment reached the compatible
             conclusion "zero components clear verified-edge + served-
             fresh"; PatchTST was RETIRED 08-02 by operator delegation;
             and GOAL-8 (operator, 08-03) superseded the premise with a
             DIFFERENT second expert — slow momentum (measured rank
             correlation ρ=+0.23 vs prod) — so this document's kill does
             not bind the GOAL-8 ladder.
EVIDENCE:  both stranded copies byte-identical (diff -rq clean); absent
           from origin/main; sizes as above.
NEXT:      none — archival. The two source worktrees are removed after
           this PR merges.
