# Progress: GOAL-2v3 Stage I-1 base models preregistered

2026-08-28, declared BEFORE the Stage I-0 gate run's result is known.
Freezes: feature set F (10-min within-session features + overnight gap
as a feature + SPY/sector context), label (13-bar within-session), bases
B0 pooled control / B1 regime / B2 sector / B3 macro-trend (the operator's
"different models per state"), K5 XGB hyperparameters verbatim (no tuning),
5 forward-chaining 6-month OOF folds with a 13-bar purge, and the life
screen (block-t >= 1.0 at h=13 on A1 blocks, dependence-adjusted per base,
overall bar only). Stage I-2 trigger: a conditioned base passes AND beats
the pooled control. Any deviation later = a new attempt in the record.
