# 2026-08-12 — re-pin the alpaca_broker twin, now that both stacks carry the fix

STATUS:   FIXED (2026-08-12). `tests/test_twin_parity.py` -> **31 passed**
          (was 1 failed / 30 passed)
          `[VERIFIED — .venv/bin/python -m pytest -q tests/test_twin_parity.py,
          run in the checkout where the sibling repos resolve]`.

WHAT:     Re-pins the `diverged_pin:alpaca_broker` entry in
          `data/twin_parity_manifest.json` via
          `scripts/check_twin_parity.py --write-manifest`. Exactly **2 keys**
          change, both under `diverged_twins.alpaca_broker`
          `[VERIFIED — flattened before/after diff of the JSON: 2 changed keys,
          `execution_sha256` and `umbrella_sha256`, nothing else]`.

WHY/DIR:  The tripwire fired because BOTH sides of this deliberately-diverged
          pair moved: the execution side from renquant-execution#41 and the
          umbrella side from RenQuant#588, each bounding the P-BROKER-CONNECT
          account read on its own stack. That is the manifest's own stated
          re-pin condition — "if the change is deliberate and reviewed on both
          stacks, re-pin".

          **Timing was the whole point of this PR being separate.** When #41
          landed alone, the tripwire went red and offered `--write-manifest` as
          the remedy. Re-pinning then would have frozen a state where the
          module the runner actually imports (`live/alpaca_broker.py`, via the
          relative import at `live/runner.py:33`) still had an unbounded
          account read — silencing a guard that was pointing at a live gap.
          The gap is now closed on both stacks and on this machine, so the
          re-pin records reality instead of hiding it.

EVIDENCE:
  artifact:      `data/twin_parity_manifest.json` (2 hash values) + this record.
                 No code.
  prod or exp:   prod — the twin-drift tripwire's reference state. It changes
                 what the guard compares against, not any runtime behaviour.
  existing data: yes — the machine's umbrella and execution checkouts were read
                 to compute the hashes; the manifest was generated by the
                 repo's own `--write-manifest`, not hand-edited. Nothing
                 generated, no live state touched.
  best-known?:   yes — regenerating with the repo's own writer is the only way
                 to guarantee the pinned hashes match what the checker computes.
                 The alternative, reclassifying the pair, would retire a guard
                 that has now caught a real gap once.
  scope:         "this is the `diverged_twins.alpaca_broker` manifest entry
                 (prod tripwire reference), vs existing best = today's stale
                 pin, which reports drift that has already been reviewed and
                 landed on both stacks. A permanently-red guard is one people
                 learn to skip, which is the failure this re-pin prevents. It
                 asserts nothing about whether the two implementations agree —
                 they are deliberately diverged; it only records their current
                 reviewed state."

NEXT: nothing pending on this line. If the pair drifts again the tripwire fires
again, which is the intended behaviour — do not re-pin without first checking
whether the drift is a reviewed change on both stacks or a fix that reached
only one, as #41 did.
