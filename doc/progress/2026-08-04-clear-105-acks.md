# 2026-08-04 — the 105 ack pair clears on its own measured condition

STATUS:    ledger transition per the acks' own clears_when contracts
WHAT:      ops/renquant104/sentinel_acks.json drops the
           com.renquant.rq105-shadow-serving and rq105-liveness entries
           (9 -> 7). The shadow-serving ack's clears_when — "#736 sync
           deploys EXIT_NOT_WIRED=4 and the job's last exit becomes 4" —
           was MEASURED SATISFIED today: after the P0 deployment synced
           the run checkout, a manual kickstart (grants-logged, operator
           directive "不等！干！") exited 4 (launchctl LastExitStatus
           1024 = 4<<8) and left the dated stamped skip on disk ("SKIP
           not-wired: no producer exists ... Stage-3, #221"). The
           liveness ack's chained condition ("that job's last exit != 1")
           is thereby satisfied too. Neither ack was WRONG — the
           condition landed, so they clear rather than being removed
           for cause.
WHY/DIR:   the ledger's own rule: an ack leaves via its clears_when, an
           expiry, or a wrongness finding — never by rot. The pinned
           counts/inventories in the two guard suites are updated with
           the dated transition (the anti-vacuity floors keep their job).
EVIDENCE:  exit-4 + stamped-skip measured 12:41 PT (session log + grants
           trail; orch#621 comment carries the deployment half); ack
           suites 318 passed at count 7.
NEXT:      the 105 lane's remaining truth is unchanged and documented:
           serving stays not-wired until the Stage-3 producer (#221)
           exists — now LEGIBLY, which was the whole point.
