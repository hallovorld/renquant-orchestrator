# 2026-08-04 — retire the legacy 103 launchd trio from the reviewed surface

STATUS:    manifest removal + bounded PENDING_UNINSTALL declaration; the
           plist rm is a follow-up machine step under a logged grant
WHAT:      ops/launchd_manifest.json drops com.renquant.daily103 /
           open103 / preclose103 (43→40 jobs). The trio is dead-but-
           declared: launchctl NOT-LOADED, logs mtime Apr 28, the
           strategy's legacy tournament frozen since April (ack ledger
           #540 dispositioned it; measurement table in orch#769 item 7's
           comment). tests/test_run_surface_drift_check.py declares all
           three in PENDING_UNINSTALL with the decision record + removal
           path, per that set's designed contract: the exact-equality
           test stays green THROUGH the pending window, the SCHEDULED
           drift scan keeps alarming "unmanifested job on disk" until the
           rm executes (the containment protocol's designed reminder),
           and the moment the plists are removed the exact-equality test
           forces these entries out in the same change.
WHY/DIR:   orch#769 item 7 disposition. The drift scan cannot see
           dead-but-declared (manifest and disk agree); retiring the
           declaration is what makes the corpse visible and forces the
           cleanup through the reviewed surface, exactly the AC2
           mechanism. No revival owner exists; operator focus is 104/105.
MACHINE STEP (not in this PR, logged when executed): the jobs are already
           unloaded, so no bootout — rm the three plists under a grant
           entry in the grants trail; then a follow-up PR empties
           PENDING_UNINSTALL (forced by the exact-equality test).
EVIDENCE:  tests/test_run_surface_drift_check.py 18 passed with the
           declaration; 380 passed across manifest/drift-selected suites
           pre-declaration except the two disk-measuring tests that this
           declaration is FOR (they were the designed red).
NEXT:      merge → rm at the next deployment window (S1 batch) with
           grants logging → follow-up PR empties the set.
