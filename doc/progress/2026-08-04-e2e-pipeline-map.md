# 2026-08-04 — the E2E pipeline map (operator directive)

STATUS:    doc-only; the single map of the full chain
WHAT:      doc/arch/e2e-pipeline-map.md — data → training updates →
           artifact governance → serving → execution → evaluation →
           promotion, one table row per link with its automation cadence
           and its MECHANICAL trust mechanism (the check that fails
           loudly), plus a dated honest-gaps section (first fallback
           promotion 13:02 today; S1 lane pending two reviews; S2 readout
           pending two reviews; fast momentum Saturday; momentum-alone is
           score-level by design).
WHY/DIR:   operator 2026-08-04: "我要的是e2e的完整执行流水线…完整的！
           可信任的！" — the trustworthy answer is a map where every link
           names its verifier, and gaps are listed as gaps.
EVIDENCE:  every status cell is dated and traceable to a merged PR, a
           grants-trail entry, or a same-day measurement in the session
           record; no cell asserts an unverified state.
NEXT:      update the map when the four in-review items land and after
           Saturday's first fast artifact; the map is the standing
           answer to "what runs, when, and why should I trust it".
