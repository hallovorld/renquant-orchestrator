# L2 cost pass — the promotion case strengthens; a suspicion corrected

STATUS:    delivered. Completes the orch#926 §6.3 mandatory step. Research
           only; engine parameters untouched.

WHAT:      doc/research/2026-08-09-l2-cost-pass.md + committed CSV + verifier
           (recursion over NET returns reproduced to 1.2e-16) + derivation
           reading the same hash-pinned input manifest as orch#926.

WHY/DIR:   No cross-arm ranking was decision-grade while arms traded free.
           Frozen cost model: 10bps one-way, name swap = 2/3 book traded,
           costs inside r_i before the transform (contract rule 4).

EVIDENCE:  artifact:      committed CSV + verifier [VERIFIED]:
             churn/day panel 0.78 / fast 0.58 / slow 0.33
             drag pp/yr panel 13.1 / fast 9.8 / slow 5.5
             net: slow +40.7%/0.96 · uniform +34.6%/1.07 ·
                  hedge +31.6%/0.92 · fast +30.0%/0.92 · panel +22.1%/0.49
           prod or exp:   experiment — same replay arms as orch#926
           existing data: supersedes the gross-only cross-arm reading
           best-known?:   yes; VISIBLE CORRECTION inside: #926 §6.3 suspected
                          the costless frame flattered mom_fast — direction
                          wrong, the PANEL churns hardest and was flattered
                          most. Corrected, not deleted.
           scope:         engine's edge over champion WIDENS net (+9.5pp,
                          Sharpe 0.92 vs 0.49); floor price persists (3.0pp
                          vs uniform); mom_slow best single arm net.

TESTS:     verifier reproduces every statistic and the net-return recursion
           from the committed CSV alone.

NEXT:      L3 clean labels (fwd forward returns, no pairing) at the same
           standard; L1 remains decision-ready awaiting the operator's σ*.
