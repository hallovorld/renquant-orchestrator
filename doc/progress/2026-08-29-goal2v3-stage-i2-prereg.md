# 2026-08-29 — GOAL-2v3 Stage I-2 preregistration (stacked meta-learner)

**Bottom line:** the I-1 trigger fired by the letter (B2 beats B0 by 0.087
block-t, #1088) but the naive reference s₀ = −r13 scores above every learned
base (4.19 vs 3.18–3.59). This PR freezes the I-2 design **before any
meta-learner is fitted**, with a pass bar harder than the parent design's
minimum: the stack must beat the best base AND s₀ on the same meta-OOF rows.
Outcomes are enumerated (PASS / FAIL-A / FAIL-B / REFUSED); FAIL-A and
FAIL-B pause the line for an operator decision.

Decision needed from the reviewer: whether the tightened bar (P3) is an
acceptable *strengthening* of the merged design (it adds a condition; it
removes none) and whether the nested-OOF layout (§2, meta-OOF period
2022-07..2024-06) is the literal reading of "same OOF discipline".

Files: `doc/design/2026-08-29-goal2v3-stage-i2-prereg.md` (the prereg).

Non-goals: no harness, no fit, no bar changed after observation; the sealed
window is untouched.

Memory tier: MID (research line state) — the I-1 result and this prereg are
recorded in the design/research docs; no LONG-ledger constraint changes.
