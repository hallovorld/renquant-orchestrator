# 2026-08-04 — GOAL-8 S2 preregistration (doc-only, frozen on merge)

STATUS:    S2 (returns comparison) prereg; freezes BEFORE the S1 lane's
           first session so no arm outcome exists when criteria are set
WHAT:      doc/research/2026-08-04-goal8-s2-comparison-prereg.md — three
           arms mapped to their MEASURED record surfaces (2026-08-04
           probe: prod=trade+score, momentum-alone=score-only in-process,
           blend=e2e trade+score), which forces the frozen PRIMARY metric
           to score level: per-session top-3 baskets, shared
           ticker_forward_returns join with exclude-not-impute, equal
           weight, the SAME 20-session window as S1. Ordinal verdict rule
           (promote-interest / stop / one declared extension), explicit
           no-significance-theater clause at n=20, seeded shuffled-basket
           placebo with the seed recipe fixed in the doc, and a
           written-during-but-run-after readout discipline with mandatory
           positive-control fixture runs.
WHY/DIR:   ladder discipline: freezing S2 before S1's clock starts is the
           cheapest moment — the record-shape probe already showed the
           score-level constraint, and waiting until session 20 to decide
           metrics invites outcome-aware choices.
EVIDENCE:  record-surface facts measured 2026-08-04 (probe in the session
           log + goal-8 memory); doc-only change.
NEXT:      merge = freeze → S1 batch lands (s104#85 + umbrella pins) →
           the shared 20-session window starts at the S1 boundary →
           readout script written+reviewed during the window (fixture
           runs only) → session 20 → verdict published honestly.
