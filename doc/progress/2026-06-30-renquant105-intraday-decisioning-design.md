# renquant105 intraday-decisioning architecture — design

STATUS: RFC under review; **r12** — rollout-boundary fix (Codex r11 review): Stage-1 live exposure FROZEN at the pre-declared canary envelope; expansion / go-live now needs a separate economic authorization. Engineering design only (the prerequisite); no build yet — design first.

REVISION r12 (2026-06-30): Codex re-reviewed r11 (`f08d6d50`) and **acknowledged the r11 convergence** (statistics split out, Stage-1 downgraded to operations-only). **One rollout-boundary blocker remained**, addressed here **DESIGN docs only (no code)**: §9.3 let the live canary "expand cautiously on continued clean ops" and stated go-live does not depend on execution-quality / economic evidence — but **operational correctness (orders emitted safely) does NOT prove that moving entries intraday is economically acceptable**, so once the canary *expands* it stops merely collecting bounded pilot data and starts deploying an **unvalidated timing policy to more capital**. Fix (RFC §9.3 rewritten + new §9.3a): (1) **Stage-1 live exposure is FROZEN at the pre-declared allowlist + notional cap, solely for data collection** — the "expand cautiously on continued clean ops" language is removed; (2) **NO expansion beyond that envelope and NO general go-live** until EITHER the deferred simplified experiment-prereg PR (§9.4) uses the collected pilot data to supply an **EXPLICIT AUTHORIZING decision**, OR the operator **explicitly accepts the economic risk in a SEPARATE RECORDED decision**; (3) the canary envelope is **bounded explicitly** — a maximum **DURATION** (proposed 20 live canary sessions), a cumulative **LOSS BUDGET** (proposed 1.5% of equity), and a **STOP CONDITION**: envelope-exhaustion without a §9.3a authorizing decision → **HARD halt, revert to 盘后 batch (kill switch default-OFF)**, never silent continuation; (4) the distinction is kept crisp — **operational-correctness acceptance (§9.3: safety / idempotency / reconciliation / Tier-1 halt) gates whether the frozen canary may RUN AT ALL; it NEVER authorizes expansion or go-live** (that needs §9.3a). This keeps the engineering RFC separable from the (deferred) statistics without reviving the stats loop. Also merged `origin/main` (docs-disjoint) to keep CI green. Point-by-point map = RFC §21. Not merged, not approved.

REVISION r11 (2026-06-30): **CONVERGENCE — split out the Stage-1 statistics; Stage-1 downgraded to operations-only** per an agreed decision between the operator, this author, and Codex to STOP the statistics-patch loop (r6→r10, all on the Stage-1 synthetic-baseline non-inferiority protocol). The agreement: the synthetic batch fill is a **diagnostic reference, not a real control group**; there are **not yet enough independent trading sessions** to estimate the paired variance; and the **10-bps threshold / required sample size cannot be settled on paper before data exists.** Changes, **DESIGN docs only (no code)**: (1) the RFC keeps ONLY the settled **ENGINEERING** (盘中 real-time decision loop, real-time data plane, live model serving, real-time state/gate consistency, entry-timing logic, and the **SAFETY envelope** — idempotency / reconciliation / quantity-accounting / kill-switch); (2) the over-designed statistical protocol file `doc/design/2026-06-30-stage1-synthetic-baseline-prereg.md` is **DELETED from this PR** via `git rm` (git history preserves it) and all references that treated it as a **blocking** artifact are removed; (3) Stage-1 is **reframed OPERATIONS-ONLY** (§9 rewritten) — it runs shadow/readonly then a cautious canary to **accumulate real paired-session execution data**, **may not claim an execution-quality PASS**, the synthetic baseline is a **DIAGNOSTIC REFERENCE ONLY** (never the sole causal evidence for going live), and **go-live/rollout does not depend on any paper-designed statistical gate**; (4) the formal execution-quality A/B is **DEFERRED** to a **separate, SIMPLIFIED experiment-prereg PR** finalized against **real pilot variance** — sample size, block length, and whether a 10-bps effect is even identifiable at this account scale, none settleable on paper — so the **bootstrap / power / variance machinery is NOT carried in this RFC** (RFC §9.4); (5) the phased rollout stays, but Phase-1/Stage-1 = operations-only shadow data collection with **no execution-quality verdict** (the formal A/B is future work gated on real data). The r4–r10 statistical revision maps + the deleted prereg file are **preserved in git history**; RFC §19 records the split-out and RFC §20 the 6-point convergence map. Point-by-point map = RFC §20. Not merged, not approved.

REVISION r10 (2026-06-30): Codex re-reviewed r9 (`a570aa62`), still CHANGES_REQUESTED, but confirmed the r9 changes (removing the anti-conservative low-diversity redraw, the independent-session readiness threshold, the serial-dependence-triggered block replacement) are **substantive improvements** — **one** blocking finding remained, addressed here **DESIGN docs only (no code)**: **the readiness power calculation uses the WRONG variance for the estimand.** The gate estimates `Δ = median(IS_intraday) − median(IS_batch_synthetic)`, but r9 planned `S_ready` from `σ̂_sess`, the pre-canary 104 next-open **batch-fill** calibration variance — where **no intraday arm exists**. That batch-only variance cannot estimate the session-cluster variance of the **paired** intraday-minus-batch statistic (it omits the intraday execution variance and the within-pair covariance), so plugging it into `S_ready` can materially **understate** the required canary duration; the achieved-half-width check is unstable at the 20-cluster floor and cannot repair an invalid *planning* variance. Fix (prereg **§5.2 rewritten**, §5.4/§5.5/§5.6/§3.6/§0/§7.1 re-framed): pre-register a **conservative, estimand-matched variance source** — **primary = a BLINDED INTERNAL PILOT**: after a frozen **`S_pilot = 12`** distinct canary sessions, re-estimate the **session-cluster variance of the paired per-session Δ statistic** `σ̂²_paired,pilot` **blinded** (variance-only; the pilot treatment-effect estimate is not inspected/reported/used — preserves the one-sided Type-I error, Friede & Kieser), then apply a pre-specified **sample-size re-estimation** `S_ready = max(S_floor=20, S_pilot, ⌈(t_{0.95,S_pilot−1}·σ̂_paired,pilot/HW_target)²⌉)` with an **absolute minimum cluster count `S_floor = 20`** that cannot be undercut; **alternative / pre-pilot planning value = an externally-justified INFLATED upper bound** `σ̄²_paired = 2.0·σ̂²_sess,batch` for the omitted intraday variance + within-pair-covariance uncertainty. **Exact estimators stated:** median = **Harrell–Davis**, cluster variance = **between-session** sample variance of `{d_s}`, finite-cluster interval = **session-cluster (block) bootstrap** with the finite-cluster `t_{0.95,·}` in the planning closed form. **Batch-only calibration variance explicitly DE-REGISTERED as power evidence** for the paired Δ (retained only as the §5.2(B) planning-bound input + the §5.4 serial-dependence proxy, re-checked on the blinded pilot paired series). The pilot-re-estimated variance + updated `S_ready` + pilot block length join the **internal-pilot outputs** (computed at the pilot readout, then sealed); `S_pilot`/`f_inflate`/`S_floor`/`HW_target`/SSR-formula/`t_{0.95,·}` stay `<<FROZEN AT CALIBRATION>>`; everything else unchanged. Point-by-point map = RFC §24. Not merged, not approved.
**CI (now resolved on-branch):** the shared weekly-APY CI fix — separate **PR #211** (`fix(weekly-apy): make monitor as-of injectable ...` + bound `read_recent_rows` window) — is now on `main` and has been **merged into this branch** (`git merge origin/main`, disjoint from the docs). The required `test` check therefore runs against the fixed code; this r10 revision is a **docs-only diff** (design/progress `.md` files, no code) and did not cause the earlier red.

REVISION r9 (2026-06-30): Codex re-reviewed r8 (`b30dd7a4`), still CHANGES_REQUESTED, but confirmed the r8 **joint calibration+evaluation bootstrap** (disjoint calibration/evaluation universes, preserved within-session pairing) is **materially addressed** — **one** blocking finding remained, addressed here **DESIGN docs only (no code)**: **the evaluability floor (`M_admit_min=10` pairs / `S_eval_min=3` sessions) is arbitrary and far too weak** for a one-sided 95% bootstrap non-inferiority decision about a 10-bps margin — session-clustered fills are not independent observations, a percentile bootstrap over ~3 clusters has no credible tail resolution/coverage, and discarding/redrawing low-diversity resamples conditions away valid sampling variability (anti-conservative). Fix (prereg **§5/§7 evaluability logic rewritten**): (1) **gate readiness derived upfront from the independent-session count + a pre-canary precision/power calculation** on the **calibration session-level cluster variance `σ̂²_sess`** — pre-registered target one-sided CI half-width **`HW_target = 4 bps`** (≥~0.8-power equivalent at true `Δ=0`) and minimum independent sessions **`S_ready = max(20, ⌈(1.645·σ̂_sess/HW_target)²⌉)`**; **NON-EVALUABLE until `S_distinct ≥ S_ready` AND achieved half-width ≤ `HW_target`**, with a pre-registered not-met response (stays non-evaluable → operations-only; extend the canary under the frozen procedure or fix the cause; **never force PASS/FAIL from underpowered data**); (2) **REMOVED the discard+redraw of low-diversity resamples** as the evaluability mechanism — any discard confined to **pure numerical degeneracy** (undefined `Δ*` only; tiny `M_admit_min=2`; low-diversity-but-computable resamples **retained**); (3) **justified/replaced the 1-session block** via a pre-canary **serial-dependence diagnostic** (Ljung–Box lags 1…5 + Wald–Wolfowitz runs test at `α_dep=0.10`; fail → **stationary block bootstrap** with a data-driven `L*` from Politis–White 2004, + a gate-time canary re-check as a fragility flag). Joint bootstrap / disjoint universes / `B=10000` / `rng_seed=20260630` / one-sided-95% stay frozen; the new fitted numbers (`σ̂²_sess`, `S_ready`, diagnostic outcome + block length) join `k_spread`/`k_auction`/caps as `<<FROZEN AT CALIBRATION>>` — prereg §5 (§5.1–§5.6 rewritten) + §0/§3.6/§7.1 cross-refs, RFC §9.2c uncertainty bullet + §9.3. Point-by-point map = RFC §23. Not merged, not approved.
**CI dependency (NOT caused by this diff):** the PR's required checks are independently **red on two pre-existing weekly-APY tests** (553 pass / 2 fail). The fix is a **separate PR — #211** (`fix(weekly-apy): make monitor as-of injectable so time-dependent tests stop rotting`, the weekly-APY look-ahead fix). This r9 revision is a **docs-only diff** — three design/progress `.md` files, no code touched — and did not cause the failure, but the shared CI checks **must be green before merge**. The merge is blocked on landing **#211**, not on this revision's content.

REVISION r8 (2026-06-30): Codex re-reviewed r7 (`9f03becc`), still CHANGES_REQUESTED, but confirmed the r7 freeze / detached-fingerprint / frozen-procedure / honest-sensitivity-label fixes are **materially addressed** — **one** blocking finding remained on the Stage-1 uncertainty gate, addressed here **DESIGN docs only (no code)**: **prereg §5 bootstrapped calibration-coefficient uncertainty but NOT evaluation-sample uncertainty** — it re-fit the synthetic model on resampled calibration sessions yet computed every `Δ*` over the **FIXED** canary/admitted set, so the one-sided 95% upper CB captured coefficient error only and was **too narrow to support the +10-bps gate**. Fix: §5 rewritten as a **joint (nested) session-block bootstrap** propagating **both** sources over **two disjoint universes split at the readonly/canary boundary** — an **outer** resample of the pre-readonly calibration sessions re-fits `(k_spread,k_auction)`, paired with an **independently drawn** (separate RNG substream, never index-coupled) **inner** block-resample of the **canary** sessions, matched-**pair** block structure preserved; the **exact cross-fit rule** is pinned (disjoint universes → **no canary session ever enters a calibration fit**; independent draws), and a **degenerate-resample rule** is pre-registered (`M_admit_min=10` admitted pairs / `S_eval_min=3` distinct sessions per evaluation resample → discard + redraw ≤ `R_redraw_max=100`; base set below threshold or redraws exhausted → gate **NON-EVALUABLE**, operations-only, as with `c_max`). Block length (1 session), `B=10000`, `rng_seed=20260630` (spawned into 2 child streams), one-sided-95% stay frozen; only `k_spread`/`k_auction`/caps remain to be populated — prereg §5 (+ §2/§3.6/§7.1 cross-refs), RFC §9.2c uncertainty bullet. Point-by-point map = RFC §22. Not merged, not approved.
**CI dependency (NOT caused by this diff):** the PR's required checks are independently **red on two pre-existing weekly-APY tests** (553 pass / 2 fail). This revision is a **docs-only diff** — three design/progress `.md` files, no code touched — and did not cause the failure, but the shared CI checks **must be green before merge**. The merge is blocked on that pre-existing/shared test fix, not on this revision; flagging so the gate isn't attributed to the r8 content.

REVISION r7 (2026-06-30): Codex re-reviewed r6 (`2e5e438e`), still CHANGES_REQUESTED. It agreed r6's censoring resolution is correct (ITT over the admitted pre-treatment set; the filled-only statistic is no longer the promotion gate) but left **four blocking findings** on the Stage-1 pre-registration — "still not a frozen or executable experiment contract." All four addressed, **DESIGN docs only (no code)**: (1) **freeze before READONLY, not just canary** — readonly already reveals pair composition, opening references, missingness and modeled outcomes, so post-readonly tuning is a leak; the calibration/freeze PR must MERGE before readonly, readonly *and* canary run-creation **fail closed** on an unfrozen (`STATUS ≠ FROZEN`) or fingerprint-mismatched artifact, and the artifact is relabeled a **FREEZE TEMPLATE** (its procedure is pinned now, only fitted numbers remain) — RFC §9.2c/§9.3, prereg §0/STATUS/Gate-readiness. (2) **detached, single-algorithm fingerprint** — one algorithm (SHA-256 of the file's committed bytes; the "sha256 / git-blob" ambiguity removed), stored in a committed **sidecar** and stamped in the ledger, **never written inside the hashed file** (kills the self-reference); run-creation recomputes and requires equality with sidecar + ledger + FROZEN — prereg §6, RFC §9.2c. (3) **freeze the calibration + uncertainty PROCEDURE now** — pinned: origin regression of signed IS on half-spread + auction-slippage, **constrained Huber** estimator (box `0≤k_spread≤1`, `k_auction≥0`, projected IRLS), **3 dollar-ADV buckets** + `N_bucket_min=200` pooling, **winsorize 1/99** outlier policy, explicit **imbalance→price transform**, **one-sided 95%**, **session-block bootstrap** `B=10000` seed `20260630`, coefficients **re-fit per replication** (no assumed-Gaussian draw); only `k_spread`/`k_auction`/caps remain to be populated — prereg §3/§4/§5, RFC §9.2c. (4) **rename the mislabeled "worst-case (Manski) bound"** to a **chosen adversarial censoring-sensitivity scenario** (5% of observed outcomes lie beyond each cap; the pre-canary batch distribution need not bound intraday outcomes), add a **required cap-severity grid {90/95/97.5/99}↔{10/5/2.5/1} + tipping-point** report (fragile PASS flagged), and **reconcile** prereg §4's "never imputed" no-quote wording with the §7 imputation (`IS_cap_lo`) — RFC §9.2d, prereg §4/§7. Point-by-point map = RFC §21. Not merged, not approved.

REVISION r6 (2026-06-30): Resolves the one item r5 left open — Codex's round-4 **censoring note** (no-fill / no-trigger pairs are NOT missing-at-random, so reporting censoring counts alone is insufficient when a filled-only IS median drives promotion; the worst intraday executions can disappear). r5 had acknowledged-but-DEFERRED it as out of that revision's 2-fix scope; r6 closes it. New **§9.2d**: the execution-quality estimand is now **intent-to-treat (ITT) over the admitted pre-treatment pair set** — no admitted pair is dropped — and the gate is a **worst-case (Manski-style) bound** instead of a filled-only median: each censored **intraday** cell is imputed to a frozen adversarial cap `IS_cap_hi`, each censored **batch** cell to a frozen favorable cap `IS_cap_lo`, so Δ = (intraday − batch) is maximised **against** PASS; **PASS requires the worst-case one-sided upper CB of Δ to stay below the +10-bps margin**. The **complete-case** Δ is reported alongside and must agree (complete-case-PASS / worst-case-FAIL → NOT a PASS — exactly the cherry-pick the gate catches), and a frozen **`c_max` = 10%** max-censoring precondition makes the IS gate non-evaluable (operations-only) above it. `IS_cap_hi` (95th pct) / `IS_cap_lo` (5th pct of the pre-canary §3 calibration IS distribution) / `c_max` are added to the frozen synthetic-baseline pre-registration (new artifact §7) as researcher DOF — frozen before canary, never from canary data, covered by `synthetic_baseline_prereg_sha`. §9.1 / §9.3 reference the new gate; the §19 deferred-footnote is flipped to RESOLVED and a §20 round-4-censoring map is added. The two r4 blockers were already resolved in r5 (synthetic-baseline pre-registration §9.2c; two-tier reject gate §9.3) — r6 changes neither. **No genuinely-deferred item remains.**

REVISION r5 (2026-06-30): Codex re-reviewed r4 (`9365361`), still CHANGES_REQUESTED, with 2 remaining experiment-design blockers (it agreed the r4 quantity-accounting fix is correct and the observability limitation is now stated honestly). Operator scoped this revision to fix ONLY those 2, precisely: (1) **pre-register the synthetic batch baseline** — the §9.2b synthetic fill was under-specified (researcher DOF could move the baseline by more than the 10-bps gate, choosable after seeing fills), so added new **§9.2c** + a committed, frozen-before-canary artifact `doc/design/2026-06-30-stage1-synthetic-baseline-prereg.md` freezing the opening reference field + timestamp (primary-listing opening-auction print, NBBO-midpoint fallback), the fill formula + all parameter values (`k_spread`/`k_auction`), the calibration set + a strictly-pre-canary cutoff, per-component treatment (spread/auction-imbalance/latency/fees/rejects/no-quote — each handled or censored), the uncertainty method + a **CI-must-exclude-the-margin** overlap PASS rule, and an immutable content sha (`synthetic_baseline_prereg_sha`) stamped in every synthetic ledger row — until frozen, Stage 1 validates **operations only**, never a comparative execution-quality PASS; (2) **resolve the rollout-threshold inconsistency** (§9.3) — expansion was allowed at N≥20 while the Clopper–Pearson reject gate was non-operative until M_reject=50, so the system could expand before reject performance was evaluable; adopted **option (b)**: a two-tier reject gate where a small-sample **HARD** guard (zero critical rejects caused by invalid order/state/contract + ≤1 non-critical reject in the first N) **is operative immediately and CAN block** expansion at N≥20, with the CP rate test **reserved for scale-up** at M_reject=50 — removed the "informational only / cannot block or pass" framing; thresholds now consistent across the N≥20→M_reject window. Round-4 map = §19. (Codex's additional censoring note — no-fill pairs not missing-at-random — is acknowledged but deferred: outside the operator's 2-fix scope; §9.3 already censors+counts no-fill pairs, an ITT/penalty bound is left to the next revision and is NOT claimed resolved.)

REVISION r4 (2026-06-30): Codex re-reviewed r3 (`320b4fc`), still CHANGES_REQUESTED, 3 round-3 correctness fixes — all accepted: (1) split the **economic** quantity invariant (`target_qty = cum_filled + open_qty + remaining_unsubmitted`) from the **attempt/audit** invariant (`gross_submitted_qty = cum_filled + open_qty + cum_canceled + cum_rejected + cum_expired`) so cancel/retry can't violate the accounting and retries provably can't overfill (§7); (2) made the Stage-1 execution-quality A/B operationally observable — committed option (a): one **real** intraday fill vs a **synthetic** quote-based batch fill model with labeled uncertainty, readonly shadow explicitly NOT a fill-quality control (§9.1/§9.2b); (3) defined the §9 denominators precisely (reject = rejected/attempted, partial = partial/accepted, IS = filled-with-valid-arrival-quote, matched-pair = pre-treatment eligible w/ censoring) + a min-count/confidence reject gate. Round-3 map = §18.

REVISION r3 (2026-06-30): Codex re-reviewed r2 (`268c0af`), still CHANGES_REQUESTED, with 8 round-2 points — all legitimate (real contradictions/gaps r2 introduced), so all 8 accepted, no pushback needed this round. Fixes: (1) split `parent_intent_id` (dedup key) vs `child_order_id=parent:attempt_n` (broker client-order-id) + cumulative-qty invariants — resolves the "one id can't be both unique and reused for a remainder" contradiction (§7); (2) rewrote the data-time contract into 4 PIT input classes (frozen-signal / session-start-PIT / live-state / timing-quote), each with cutoff+fingerprint, replay inspects all four — fixes the "pre-market revision is later than T-1 close" inconsistency (§6); (3) separated implementation-shortfall (Stage-1 execution readout, vs each path's own arrival quote) from timing-economics (deferred counterfactual) — stop calling price-vs-next-open "execution cost" (§9.1); (4) committed concrete pre-registration K=5/N≥20/10-bps tolerance + denominators (§9.3); (5) matched pairs from pre-treatment snapshot only, key (signal_session,symbol,signal_version), baseline=session-T open — no post-treatment selection (§9.2); (6) safety interaction rules (most-restrictive-wins, pending counts vs deployment, sells consume turnover-not-deployment, partials reserve remainder) + timer-driven pending cancel (§10); (7) relabeled fractional shares a DEFERRED Stage-2 dependency (not a BLOCKER) — true Stage-1 BLOCKERs = quote entitlement/open-orders API/ledger cadence (§11); (8) new session-boundary policy §11b (first-tick open+5min, close−30min cutoff, early-close, halt/LULD, stale-feed→sell-only, DAY-only no-carry). Round-2 map = §17.

REVISION r2 (2026-06-30): Codex requested changes on `c976ed8` with 7 blocking design-completeness points. Revised the RFC to address all 7 — accepted 1,2,4,5,6,7 in full; refined 3. Added: §6 data-time/as-of contract + replay no-leak test (the Stage-1 score is frozen at T-1 close, so leakage is provable, not asserted); §7 order-lifecycle state machine + `intent_id` idempotency key + reserved-cash accounting (the dedup-vs-pending #1 risk); §8 per-repo build slices (execution → pipeline → orchestrator) with merge order + acceptance tests, respecting CLAUDE.md boundaries (orchestrator flips last, default-OFF); §9 measurement plan; §10 conservative safety-envelope defaults; §11 dependencies (fractional-share chain marked a BLOCKER, removed from §2 available-primitives). Refinement on #3: Stage-1 gate is pre-registered OPERATIONAL/execution-quality correctness, with the heavyweight alpha estimand explicitly DEFERRED to Stage-2 scoping rather than pre-registered up front — reconciles Codex's own #3 (rigorous experiment) vs #7 (Stage 1 is plumbing, not an alpha claim) and honors the over-engineering-validation-before-alpha lesson.

WHAT: An RFC (`doc/design/2026-06-30-renquant105-intraday-decisioning-architecture.md`) that reframes renquant105 as an ENGINEERING evolution from 104's 盘后 (after-close batch) decisioning to 盘中 (real-time, during-session) decisioning, and specifies the target architecture, the staged evolution path (Stage 1 = decouple entry timing from the batch by repointing the existing intraday loop from sell-only to full decisioning), engineering contracts/risks, and the explicit downstream boundary (model rework is NOT the prerequisite).

WHY/DIR: Operator re-clarification 2026-06-30 — I had drifted a whole session into alpha/signal search; 105's core is the engineering of real-time intraday decisioning, the model is downstream, and **the prerequisite is the engineering design.** Build on prior work, do not rewrite: the RFC is reuse-first (intraday loop, InferencePipeline, gate-stack, model serving, decision-ledger, decision-tree-review, safety mechanisms, methodology spine all carried over). Goal = catch the trend entry in real time during the session instead of the next-day batch; holding period stays multi-day (not HFT/day-trading).

EVIDENCE:
- §4(b): artifact = `doc/design/2026-06-30-renquant105-intraday-decisioning-architecture.md` (this PR, orchestrator); prod-or-exp = DESIGN/RFC (no code, no live change); existing-data = current 104 flow (`daily_104.sh` after-close batch + `com.renquant.intraday` sell-only loop) is the documented baseline; best-known? = first written architecture for the 盘后→盘中 evolution under the corrected (engineering-first) framing; scope = orchestrator control-plane design only, model rework explicitly out of scope (Stage 3 / downstream).
- Today's alpha screens (directional / analyst-orthogonal / minute / breadth) were all honest NULLs — recorded as the WRONG question for 105's prerequisite, not as 105 progress.

NEXT: Operator/Codex re-review of the five open questions (cadence, entry-timing baseline framing, safety-envelope defaults, the operational-gate-vs-alpha-pre-registration framing, per-repo decomposition/ownership). On agreement, scope Stage 1 as three ordered per-repo PRs (execution lifecycle → pipeline runtime/sizing → orchestrator scheduling, default-OFF → readonly → canary), gated on **operational correctness ONLY** (no-leak + idempotency + reconciliation + the Tier-1 critical-reject safety halt + session boundaries) — execution-quality (IS) accrues in the decision-ledger as a **diagnostic** for a future, separate **simplified experiment-prereg PR** finalized against real pilot variance (RFC §9.4), not as a Stage-1 pass/fail; verify the fractional-share-chain blocker (execution #19 / pipeline #153 / strategy #36) before assuming whole-share-only is temporary.
