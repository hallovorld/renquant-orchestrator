# Progress: independent design review of the 104/105 RFC set

DATE: 2026-07-01
PR: design(review) — amendments to #208 / #210 / #212 + umbrella `strategy-104.md`
ARTIFACT: `doc/design/2026-07-01-104-105-design-review-amendments.md`

## What

Operator asked for a deep independent review of the merged 104/105 design set. Reviewed
#208 (intraday decisioning RFC, r12), #210 (model freshness governance, R6), #212 (shadow
scorer freshness, r2), the 105 direction decision (06-28), and umbrella `strategy-104.md`.
Findings consolidated into one amendment RFC (docs only), filed as a new PR per the
freeze-merged-designs rule.

## Key findings (severity-ordered)

1. **A1 (blocking):** #210/#212's fast-axis freshness ceiling (21–45d grid; 35d shadow tier)
   is structurally unsatisfiable for fwd_60d-label models (achievable training cutoff is
   always ~88 calendar days behind). Same failure family as the fund-freshness serving-axis
   clip bug. Fix: split the freshness key into serving-feature-axis freshness vs
   training-label frontier distance.
2. **A2 (blocking):** #208 omits PDT / account-type / same-session round-trip constraints on
   a sub-$25k account. Fix: account-type blocker + day-trade budget (proposed 0) in the
   Stage-1 envelope + cash-settlement accounting in §7.
3. **A3:** #210's Final is experiment-only-authorized while its own §5.0 audit will likely
   fail closed → adopt #208 §9.3a's two-path authorization; Fix-3 placebo failures
   fail-closed until the difference test exists.
4. **A4:** Stage 1–2 is execution-timing repositioning of a frozen signal, not
   "catch-the-entry-as-it-forms" (that is Stage 3); Stage-2 estimand must cite the phase −1
   intraday-alpha NO-GO (PR #199) and pin the timing-residual estimand.
5. **A5:** pilot-data integrity pins — gate-input census, order-type pre-declaration,
   SIP-vs-IEX feed, loss-budget noise probability (~coin-flip halt at $157), ~1,800-pair
   identifiability arithmetic, batch-rotation churn diagnostic, active-path audit
   (fractional-shares lesson).
6. **A6:** umbrella `strategy-104.md` production snapshot stale since the 06-23 XGB
   re-promotion (the premise rot that broke #210 R1) → machine-generate the snapshot
   (umbrella follow-up PR).
7. **A7:** the direction-decision's regeneration PR is the evidence base of the whole 105
   direction; GO criterion (d) is near-certain to fail given BEAR-only skill → carve-out
   question.

## State / next

- Design doc + this progress doc are the whole PR (no code/config change).
- Next: operator + Codex review; on agreement, per-doc amendment PRs in the order given in
  the design doc (§ proposed amendment order); umbrella snapshot-generation PR separately.
