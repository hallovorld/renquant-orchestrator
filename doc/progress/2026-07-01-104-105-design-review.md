# 104/105 design-review amendments — design PR

STATUS:   design for review (docs only — no code/config/broker/risk/sizing change in this PR).
          Describe → discuss → Codex review → then per-doc amendment PRs.
REVISION: r3 — addresses Codex's r2 review (CHANGES_REQUESTED), which ACCEPTED the r2 design fixes
          (superseded-PDT premise corrected, exits-always-allowed restored, Track-A prereg
          preserved, #213 horizon-aware monitor acknowledged) and left TWO blockers: (1) this
          progress record still preserved the r1 (withdrawn) claims and lacked the control-schema
          literal fields — rewritten to the CURRENT conclusions in schema form; the design doc's
          amendment-order item 7 renamed from "prereg carve-out" to "separate frozen
          BEAR-risk-switch preregistration". (2) A5.4/A5.5 overstated assumption-only arithmetic
          as operational probability/identifiability — rewritten as sensitivity SCENARIOS with
          explicit formula + assumptions, plus pre-registered pilot-variance / cluster-unit /
          alpha-power / attrition / blinded sample-size-re-estimation requirements for §9.4, and
          the underpowered outcome explicitly labeled operator RISK ACCEPTANCE (never evidence of
          economic benefit). Prior: r2 (head `e313cd48`) fixed A1/A2/A7 per Codex r1; r1 (head
          `5f37d241`) initial review.
WHAT:     an independent deep review of the merged 104/105 design set — #208 (intraday
          decisioning RFC, r12), #210 (model freshness governance, R6), #212 (shadow scorer
          freshness, r2), the 105 direction decision (06-28), and umbrella `strategy-104.md` —
          consolidated into one amendment RFC:
          `doc/design/2026-07-01-104-105-design-review-amendments.md` (findings A1–A7 +
          per-doc amendments + amendment order + open questions).
WHY/DIR:  the merged design set is strong on decision provenance but carries (A1) an RFC-text /
          implementation divergence on the freshness key — the #210/#212 TEXT still specifies
          raw-age fast-axis semantics that are structurally unsatisfiable for fwd_60d-label
          models, while the shipped #213 monitor is already horizon-aware
          (`label_observation_cutoff` key + expected-lag threshold widening;
          `max_feature_anchor_date` provenance-only, umbrella #423 r3) — so the amendment is
          text alignment + per-recipe label-horizon semantics; (A2) NO broker-regulatory /
          settlement envelope in #208 — verified (read-only `GET /v2/account`, 2026-07-02) the
          live account is a margin account under FINRA's Intraday Margin Standards
          (eff. 2026-06-04; PDT designation deprecated) → envelope = verify-then-bind recorded
          regime blocker + intraday-margin/buying-power headroom (consistent with
          `non_marginable_buying_power` sizing) + EXITS-ALWAYS-ALLOWED precedence (constraints
          bind entries only); (A3) #210's Final is experiment-only-authorized while its own §5.0
          audit will likely fail closed → adopt #208 §9.3a's two-path authorization; Fix-3
          placebo failures fail-closed until the difference test exists; (A4) Stage 1–2 is
          execution-timing repositioning of a frozen signal, not "catch-the-entry-as-it-forms"
          (Stage 3 only); Stage-2 estimand pinned to the timing residual, citing the phase −1
          intraday-alpha NO-GO (PR #199); (A5) pilot-data integrity pins — gate-input census,
          order-type pre-declaration, SIP-vs-IEX feed provenance, loss-budget noise SENSITIVITY
          SCENARIO, §9.4 power-prereg requirements (pilot variance/correlation, cluster unit,
          target effect, alpha/power, attrition, blinded re-estimation; underpowered → operator
          path = explicit risk acceptance), batch-rotation churn diagnostic, active-path audit;
          (A6) umbrella `strategy-104.md` production snapshot is stale (pre-dates the 06-23 XGB
          re-promotion — the premise rot that broke #210 R1) → machine-generate it; (A7) the
          direction-decision's regeneration PR is the evidence base of the whole 105 direction;
          the pre-registered GO criteria (a)–(e) STAND UNTOUCHED — the BEAR risk-switch is a NEW
          hypothesis for a SEPARATE frozen preregistration, never a retroactive carve-out.
EVIDENCE: #213 monitor semantics verified in
          `src/renquant_orchestrator/model_freshness_monitor.py` (origin/main; umbrella #423 r3
          docstring + `DATA_CUTOFF_FIELDS` ordering + `_AXIS_EXPECTED_LAG_BDAYS` widening).
          Account regime verified read-only `GET /v2/account` 2026-07-02: margin,
          `pattern_day_trader=false` (deprecated), `daytrade_count=0`,
          `daytrading_buying_power ≈ $37.5k` on ~$10.8k equity (≈3.5×, impossible under legacy
          sub-$25k PDT), live `initial_margin`/`maintenance_margin`/`intraday_adjustments`;
          refs: Alpaca account-plans + understanding-the-new-intraday-margin-rule docs, FINRA
          weekly archive 2026-01-07. Shadow PatchTST vintage facts from #212 (served pin
          2026-05-22, `effective_selection_cutoff_date=2026-02-10`). Phase −1 NO-GO numbers from
          PR #199. σ(open→close) ≈ 150bps from the phase −1 measurement (used ONLY as an
          upper-bound scenario input in A5.5, not as the paired-residual variance).
NEXT:     Codex re-review of r3. On agreement: per-doc amendment PRs in the design doc's
          amendment order (A1 text alignment first — before anything is pre-registered from the
          RFC wording; A2 envelope second — hard blocker for any live canary session); umbrella
          snapshot-generation PR separately (A6); the separate frozen BEAR-risk-switch
          preregistration authored per the A7 open question. No implementation in this PR.
