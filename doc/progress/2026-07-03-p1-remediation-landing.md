# P1 remediation landing (items 1–2) — ops record PR

STATUS:   ops record; docs only (actions already executed under the unblock clause).
REVISION: r1.
WHAT:     doc/ops/2026-07-03-p1-remediation-landing.md — the executed restore (byte chain
          04d7a381 verified, fingerprint zero-diff), the calibrator refit (side-path
          inspection first; final C6 PAIRED; neutral −0.267 on the restored scale), and
          the 47/47 step-0 legacy pre-stamp with idempotence recheck (detonations defused).
          Items 3–5 deferred to explicit grant. Prediction registered: today's 13:55 run
          should show sign_laundered collapse to single digits without M4's flag.
WHY/DIR:  record + notify is mandatory for every unblock-clause use; two uses today.
EVIDENCE: verify script PASS output; prestamp dry-run/apply/recheck report JSONs
          (scratchpad, archived); backups retained.
NEXT:     operator grant for items 3–5; today's run = the pairing-fix A/B read; the
          scorer-identity monitor (#277, merged) install closes the detection gap.
