# 2026-07-26 — mid-term memory: PatchTST queue position (third blend leg)

STATUS:    memory-tier note only
WHAT:      doc/memory/mid-term/model-edge.md gains the operator-agreed PatchTST
           roadmap position: third blend leg AFTER (1) certified clean baseline,
           (2) cost-gated verification, (3) XGB-blend shadow active. Entry via the
           standard gate chain; no exceptions.
WHY/DIR:   PatchTST's prior record (all-negative scores, G4 KILL, prune-line
           placebo failures) means it cannot re-enter the roadmap as a peer to
           the XGB blend without first clearing the same certification chain
           that qualified model#74/#75/#76 — writing that ordering into the
           mid-term ledger now prevents a future session from re-litigating
           the sequencing or promoting PatchTST past a prerequisite it hasn't
           met. Directionally this keeps PatchTST dormant (research-only)
           until pipeline#213's shadow is active and stable; it authorizes no
           run and changes no production path.
EVIDENCE:
  artifact:      the merged model#74/#75/#76 chain (the gate machinery referenced)
  prod or exp:   documentation only
  existing data: PatchTST prior record (all-negative scores; G4 KILL; prune-line
                 placebo failures) cited as the baseline prerequisite's basis
  best-known?:   operator-agreed sequencing in session 2026-07-26
  scope:         roadmap note; authorizes no run
NEXT:      dormant until pipeline#213 shadow is active and stable.
