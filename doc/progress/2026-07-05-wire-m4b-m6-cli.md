# Wire m4b + m6 into CLI

Date: 2026-07-05
Task: CLI wiring gap (m4b_conviction_replay #385, m6_restamp #384)

## What shipped

- `renquant-orchestrator conviction-replay` — M4-b conviction-floor replay
- `renquant-orchestrator m6-restamp` — M6 fingerprint re-stamp (dry-run default)
- 2 CLI delegation tests
- Strategy snapshot updated

## Not in scope

- Job runner scheduling (both are one-off tools, not scheduled jobs)

## Round 2 (codex review)

STATUS: fixed
WHAT: `data/strategy_snapshot.json` carried a stray `tc_measurement` entry
under `source_modules` that this PR does not add or wire — `tc_measurement.py`
belongs to a separate, concurrent PR (#391, "graduate transfer coefficient to
standing measurement job"), not this branch. The prior snapshot update was
generated against a contaminated local environment.
WHY-DIR: codex correctly flagged the snapshot-alignment test failing in CI as
real signal of drift, not noise — the committed baseline claimed a module
this branch's own code doesn't contain.
EVIDENCE: regenerated via `scripts/generate_strategy_snapshot.py --update`
against a clean checkout of this branch only; diff is now a single-line
removal of `tc_measurement`. `tests/test_doc_alignment.py` passes (2/2). Full
suite 2977/2979 (2 pre-existing unrelated failures in
`test_bundle_consistency_ci_gate.py`, confirmed reproducing on clean
`origin/main`).
NEXT: none — PR is now scoped correctly to conviction-replay + m6-restamp.
