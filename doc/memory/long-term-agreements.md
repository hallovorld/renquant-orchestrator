# LONG-TERM AGREEMENTS — binding constraint ledger (C4)

> Tier: **LONG.** Changes **only on an explicit operator decision.** Codex review
> **rejects** any PR that violates an item here. On conflict, this file wins over
> mid/short-term memory. Reversals are recorded here, never silently dropped.

| # | agreement | since |
|---|---|---|
| 1 | **Never bypass the WF gate or branch protection.** A model that can't pass does not ship; fix the corpus/recipe or the model — never `RQ_ALLOW_NO_WF` / admin-merge. | standing |
| 2 | **Production paths are read-only.** Never write `data/*.parquet`, `strategy_config.json`, live model artifacts, committed WF corpora, `live_state.*` — or anything the live tree's scheduled jobs consume. Experiments live in isolated clones/worktrees only. **The rule stands unchanged; row 2a is one narrow single-use exception, not an amendment.** | 2026-06-17 |
| 2a | **ONE-TIME operator exception to row 2 — per-name concentration only.** Operator directive 2026-08-06, verbatim: **"单股上限可以是30%，最多保留8支股票"** (single-name cap may be 30%, keep at most 8 names). Authorises exactly one agent PR — **renquant-strategy-104#94** — to move `regime_params.BULL_CALM.max_position_pct` and `ranking.kelly_sizing.max_concentration` from `0.12` to `0.30` in `configs/strategy_config.json`, its golden twin, and the six live shadow lanes. **Scope is exhaustive:** no other key, no other regime (`BULL_VOLATILE` 0.20 / `CHOPPY` 0.15 / `BEAR` 0 and every sector cap keep their values), no other file, no other PR. **Does not generalise** — a later concentration change, or any other production-config write, needs its own row. Expires on merge of #94; if #94 closes unmerged this row is struck, not reused. Recorded because row 2's protection is **auditability**, not inconvenience: absent this row, the only evidence for a 2.5x live concentration raise would be an agent's own commit message asserting the operator asked for it. **Countersignature**: the Claude agent shares the `hallovorld` GitHub login with the operator (`doc/ops/agent-token-storage.md` §1), so a Claude-authored PR-thread comment cannot serve as an independent countersignature — Claude declined to post one for exactly that reason (PR #883, 2026-08-06T16:36:45Z). The row is instead validated by Codex's audit-record comment (PR #883, 2026-08-06T16:42:19Z, distinct login `haorensjtu-dev`) attesting to this directive received directly from the operator in a live Codex session, and Codex's subsequent APPROVED review (16:43:59Z) citing that same instruction to authorise merge. | 2026-08-06 |
| 2b | **ONE-TIME operator exception to row 2 — slot cap only. AUTHORIZATION COMPLETE 2026-08-26: operator confirmation of the narrowed package received and quoted below.** History: operator decision 2026-08-24, verbatim **"同意"** (Claude session), approved the THEN-CURRENT orch#1046 r1 package — cap 8→10 COUPLED WITH `execution.fractional_shares.enabled: true`. Codex review of orch#1046 then required decoupling, and verification on the active tree showed the fractional half would fail-close ALL buys (zero `is_fractionable` implementations on the live umbrella adapter; `software_stops` unarmed); orch#1046 r2 (MERGED) records the staged redesign — cap-only now, fractional later under its own chain — including that cap-10 integer tilt IMPROVES on today (1.28× → 1.20×, orch#1025 grid). Approval of A+B does not convert to first-hand approval of A alone, so the narrowed Stage 1 needs its own operator confirmation: **Operator confirmation 2026-08-26, Claude operator session (same channel as the original 「同意」), verbatim: 「确认 cap-only:8→10」 — given as item 2 of the operator's five-item authorization batch, in which the operator repeated back verbatim the standing ask "确认 cap-only:max_concurrent_positions 8→10,fractional 延后". The same confirmation is posted with timestamp on this PR and on renquant-strategy-104#100.**. Once confirmed, this row authorises exactly one agent PR — **renquant-strategy-104#100** — to move `max_concurrent_positions` from `8` to `10` in `configs/strategy_config.json`, its golden twin, and the six prod-mirror lanes (`shadow_blend`, `shadow_blend_momentum`, `shadow_momentum`, `shadow_blend_momentum_fast`, `shadow_blend_rb_mom`, `shadow_blend_rb_fast`), plus the `_max_concurrent_positions_reason` provenance note in active+golden (named explicitly per the 2a countersignature round, where an unnamed `_reason` key voided an approval). **Scope is exhaustive:** no other key (`rotation.panel_buy_top_n` stays 3; every `regime_params` cap keeps its value), no other file (frozen arms `shadow`/`shadow_a`/`shadow_b`/`shadow_vol_window` stay 8), no other PR; `execution.fractional_shares.enabled` stays `false` (its later flip needs its own dependency chain and its own row). **Does not generalise.** Expires on merge of renquant-strategy-104#100; if #100 closes unmerged this row is struck, not reused. Monitoring/rollback per orch#1046 r2 stage 1 (10-session gates: deployment, integer tilt ≤1.28×, wash-sale block-session rate; rollback = single-key revert PR + pin advance). **Countersignature**: as in 2a, a Claude-authored comment cannot countersign (shared `hallovorld` login); Codex review of this PR verifies scope and evidence only and is NOT a countersignature of operator intent — the authorization rests on the operator confirmation quoted above. | 2026-08-24 |
| 2c | **ONE-TIME operator exception to row 2 — config hygiene, fingerprint-changing but behaviorally equivalent.** Operator authorization 2026-08-26, Claude operator session, verbatim: **「授权 row 2c」** — item 3 of the operator's five-item authorization batch, in direct response to the standing ask naming renquant-strategy-104#102 (removal of the dangling `walkforward.manifest_path`). Authorises exactly one agent PR — **renquant-strategy-104#102** — to remove the dangling `walkforward.manifest_path` key from the EIGHT config carriers its diff touches (`configs/strategy_config.json` + seven shadow carriers; the golden config never carried the key and is covered by the test assertion only). **Identity note:** the change is behaviorally equivalent (the key is read nowhere — code-path analysis in the PR) but NOT fingerprint-inert: strategy-config fingerprints in run bundles and provenance gates change intentionally. **Scope is exhaustive:** no other key, no other file, no other PR. **Does not generalise.** Expires on merge of renquant-strategy-104#102; if #102 closes unmerged this row is struck, not reused. Deployment follows the normal ordered umbrella pin advance + runtime sync as separate reviewed steps. **Countersignature**: as in 2a/2b, a Claude-authored comment cannot countersign (shared `hallovorld` login); the authorization rests on the operator confirmation quoted above, posted with timestamp on #102 as well. | 2026-08-26 |
| 2e | **ONE-TIME exception to row 2 — `rotation.enabled` only. EVIDENCE-BACKED; the first-hand, change-specific operator confirmation slot is PENDING (operator may confirm or veto; the agent proceeds under the max-benefit directive with a single-key revert).** Finding (three read-only audits, 2026-08-29/30): (i) **parity audit** — the served recipe's WF gate metadata records **0 rotation trades in all 3 validated cuts**, so no WF number has ever covered the rotation engine [VERIFIED — prior work, `metadata.wf_gate_metadata` on the served panel artifact; memory `live-policy-is-not-the-validated-policy`]; (ii) **logic forensics** — `net_adv` compares per-ticker `expected_return` values that are a 5-day RSI/MACD/CCI/BBP/ADX model's calibrated 5d ER ×12 to the 60-day horizon (pipeline `models.py:113-115`, `task_candidates.py:349-361`); across live sessions **22% of session pairs jump ≥ the 0.06 `min_expected_advantage_pct` threshold** and 12 names show **17 sign flips**; `transaction_cost_pct=0` prices no round-trip [VERIFIED — prior work, `candidate_scores` vs per-ticker ER; memory `flipflop-driver-is-5d-oscillator-er`]; (iii) **ledger forensics** 07-17..08-28 — **33 round-trips realized +$4.32** (16 W / 17 L, median hold 2 d), gross traded $60.1k = **5.55× turnover in 6 weeks**; churn set (APH/CRWD/NET/NVDA/SPG/VLO) = 55.6% of gross traded $; exit-reason P&L: rotation 8 trips +$64, stop_loss 2 trips −$207 — both RE-ENTRIES within days of an exit [VERIFIED — prior work, broker portfolio history + FIFO over fill activities; memory `win-rate-is-backtest-not-live`]. **Authority basis:** operator directive 2026-08-30, Claude operator session, verbatim 「你问的6个问题基本都不是真正的问题!你自己按照受益最大方向推进」 ("the six questions you asked are basically not real questions — push forward yourself in the direction of maximum benefit"), PLUS the three evidence pointers above. Per the 2b/2d precedent a directive that names no key is NOT first-hand approval of this key; unlike 2d's blanket directive, this one explicitly delegates the direction to the agent, so the package proceeds under it instead of waiting, with the revert below as the operator's veto path. **Operator confirmation slot: [FILLED 2026-08-30 12:09 PDT — channel: the Claude operator session (this repo, interactive); operator verbatim reply 「确认」, given in direct response to the agent's prompt that named both keys explicitly: 「确认 row 2e:rotation.enabled=false;确认 row 2f:execution.buying_power_mode=settled_cash」. The reply and its prompt are quoted together because the one-word confirmation is change-specific only through the prompt it answers; posted with timestamp on the orchestrator PR carrying this row and on renquant-strategy-104#105.]** This row authorises exactly one agent PR — **renquant-strategy-104#105** (branch `config/rotation-off-unvalidated-churn`, commit 8aa2171 at preparation) — to move `rotation.enabled` from `true` to `false` in `configs/strategy_config.json`, its golden twin, and the six prod-mirror lanes (`shadow_blend`, `shadow_blend_momentum`, `shadow_momentum`, `shadow_blend_momentum_fast`, `shadow_blend_rb_mom`, `shadow_blend_rb_fast`), plus the `rotation._enabled_reason` provenance note in active+golden (named explicitly, per the 2a countersignature round where an unnamed `_reason` key voided an approval). **Scope is exhaustive:** that single key — every other `rotation.*` key keeps its value (`min_expected_advantage_pct` 0.06, `target_horizon_days` 60, `min_rotation_hold_days` 7, `transaction_cost_pct` 0, `panel_buy_top_n` 3, the whole `joint_actions` block), no other file (frozen arms `shadow`/`shadow_a`/`shadow_b`/`shadow_vol_window` stay `true`), no other PR. **Does not generalise.** Expires on merge of renquant-strategy-104#105; if #105 closes unmerged this row is struck, not reused. **Expiry / restore condition: until a rotation design passes a WF gate** — re-enabling is a NEW authority row, never a silent flip; the 08-29 entry-date re-seed fix (umbrella) removes one hold-day bypass, not the unvalidated advantage signal, and does not satisfy this condition. Deployment = normal ordered umbrella pin advance + runtime sync as separate reviewed steps; day-1 check = the `daily_104` log carries no `ROTATION_SELECT`/`ROTATION_EXEC` lines while exits (stop_loss, model_protection, model_sell) still fire; rollback = single-key revert PR + pin re-advance. **Countersignature**: as in 2a–2d, a Claude-authored comment cannot countersign (shared `hallovorld` login); Codex review verifies scope and evidence only; until the slot is filled this row's authority is the delegating directive + the evidence recorded above, and the operator's veto is honoured by the revert path. | 2026-08-30 (prepared; confirmed 2026-08-30 12:09 PDT) |
| 3 | ~~XGB / GBDT is VETOED as a pitch.~~ **Pitch-veto LIFTED 2026-06-21 by operator** ("把xgb模型回归prod，把正在开发的patchtst放到shadow … 用最新数据重训xgb … self audit … 如果通过的话，重新用xgb run daily full once to validate E2E"). XGB may be pursued for prod again. NOTE: lifting the veto is **not a quality verdict** — the fresh XGB **FAILED** the WF gate (#166: regime-sanity + monotonicity; #167: BULL_CALM weak, aggregate BEAR-inflated). Any production use is operator real-money discretion behind the conviction floor (mu_floor, pipeline #140), **not a gate pass and not a bypass**. | 2026-06-17 → veto lifted 2026-06-21 |
| 4 | ~~PatchTST is the chosen model to make work.~~ **No longer the sole sanctioned model 2026-06-21:** PatchTST directed toward shadow (still under development). Neither model has a gate-passing version yet. | 2026-06-17 → revised 2026-06-21 |
| 5 | **Report bottom-line-first** (`AGENT-RETROSPECTIVE.md` §4a); **no "X works/fails" without the §4b evidence block.** | 2026-06-17 |
| 6 | **Every PR carries `doc/progress/<date>-<slug>.md`** (C5). No progress doc ⇒ Codex rejects. | 2026-06-17 |
| 7 | **Never self-merge.** Codex approval is the **mechanical** merge gate for Claude PRs — `.github/CODEOWNERS` + `require_code_owner_reviews=true` + `enforce_admins=true` (2026-06-19, #155) require the *other* agent's approval; admins can't override. Design docs are not merged while under discussion. | standing |
| 8 | **Docs/PRs/commits in English; chat in Chinese.** | standing |
| 9 | **Never delete/empty the umbrella** at `/Users/renhao/git/github/RenQuant`. | standing |
| 10 | **Every number carries a provenance tag.** In any claim, PR, design doc or memory entry, each quantity is tagged `[VERIFIED — <command/file>]` (measured this session), `[VERIFIED — prior work, <ref>]`, `[DERIVED — <formula/inputs>]`, or `[ASSUMED — <why>]`. **An untaggable number is not stated.** A number that drives a decision is **re-measured in-session** — recall is not evidence. When a document's figures change, the whole file is reconciled; corrections appear in a **visible** corrections section, never a silent overwrite. | 2026-07-28 |

_Reversals: (none yet)._
