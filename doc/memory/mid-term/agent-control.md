# Workstream: agent control contract + memory structure

STATUS:   in-progress — PR #153 (awaiting Codex review).
GOAL:     contain the systemic agent failure with external controls (not promises):
          C1 sandbox/read-only prod · C2 status+evidence templates · C3 scoped tasks ·
          C4 this memory ledger · C5 per-PR progress docs; Codex review = the gate.
NEXT:     Codex review of #153; operator decision on building the C1 prod-path
          write-guard hook (the one control that blocks a violation mechanically).
EVIDENCE: n/a (process/control change). See `../../AGENT-RETROSPECTIVE.md`.
