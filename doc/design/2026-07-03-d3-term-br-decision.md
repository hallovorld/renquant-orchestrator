# D3 — the Term-BR decision: no expansion, no down-cap; BR is closed as an IC lever on current data

STATUS: DECISION memo (D3, delegated per the #230 §1 protocol: evidence hierarchy +
adversarial verification + rollback + notify-not-approve). Codex adversarial review is this
PR's review. Operator may veto within review; absent veto the decision binds.
DATE: 2026-07-03. DECIDER: claude (leader loop), under the 2026-07-02 delegation.

## 1. The decision

1. **No breadth expansion.** Wave-based expansion is closed (M8: NO-GO at −0.0477 vs the
   −0.010 band, three seeds, verified #264 with the dilution mechanism reproduced exactly).
2. **No training-panel down-cap.** The core-shrink hypothesis is NULL one-directionally
   (#283: 18/18 member-seeds negative; controls prove the harness detects planted effects).
   The training panel STAYS at the full 142-name watchlist.
3. **Term BR is re-priced to zero as an IC lever on current data.** The G* value equation's
   √BR_eff term holds at ~131/yr [77, 500]; no funded work may claim IC gains from panel
   composition without a new pre-registered gate.
4. **The single remaining BR route stays open and gated**: M7's PRIMARY-panel measurement
   (RS-5 #282: INCONCLUSIVE on the fallback panel BY DESIGN; the frozen thresholds carry
   over unchanged). Its prerequisite is the Norgate trial → primary constituency-by-date
   panel → one-command re-measure. The trial signup is an ask-first landing item (vendor
   account), listed in §5.
5. **Book-level concentration is EXPLICITLY out of BR** — it is a TC/sizing question. The
   mechanism finding that licenses this split: selection-only restriction scored by the
   BROAD-trained production scorer reads ≈0…+0.022 (neutral-to-mildly-positive), while
   RETRAINING narrow is what destroys IC (#283 diagnostics). Any future book restriction
   must be evidenced via a production-scorer shadow replay, not a retrain study.

## 2. The evidence chain (all merged, all S-REL-audited or S-REL-native)

| Input | Verdict | Verification |
|---|---|---|
| M8 wave-1 (#261) | NO-GO decisive | UPHELD #264 + the similarity-specific correction |
| Core-shrink (#283) | NULL, 18/18 negative; size-dominance mechanism | S-REL-native (plant/null/oracle controls PASS) |
| RS-5 fallback (#282) | M7 INCONCLUSIVE-pending-primary; cost gate (b) binds, not (a) | S-REL-native (controls ×3 seeds) |
| The #264 random-wave hint | random +100 improved incumbent IC in 2/3 draws | motivated #283's frozen test — did NOT replicate as a shrink gain |

The two hedges of Term BR (expand / shrink) have now BOTH reported under frozen criteria
with adversarial or native verification. This is the plan's §2 evidence bar for a D-gate.

## 3. Consequences

- The IR = TC·IC·√BR decomposition loses its cheapest theoretical lever; the #279
  re-weighting (EXEC lane, PROCESS lane, C1 patience, sleeve) is REINFORCED — nothing in
  this decision creates new work.
- RS-5's cost-gate finding transfers a warning to every future BR/universe proposal: at
  this book scale the binding constraint is TRADING COST, not IC (gate (b) failed at net
  Sharpe −0.128 while (a) merely missed). Proposals must clear the cost gate on paper first.
- The M8/M-SIG reopening routes are unaffected (they are IC-lane, not BR-lane).

## 4. Rollback & reopening (pre-registered)

- REOPEN iff: (i) M7-primary GO under RS-5's unchanged frozen thresholds (post-Norgate);
  or (ii) PIT panel-era outcome accrual (the S8 arm re-run, ~2026-Q4) overturns #283's S8
  supporting arm with a CI-clean positive; or (iii) the book scales past the RS-3 re-trigger
  (≥$50k) — cost-gate arithmetic changes materially with size.
- ROLLBACK: this decision changes no running system (it prevents work, not creates it);
  reversal = a new dated addendum citing which reopening fired.

## 5. Actions

1. Master plan: Term BR row → CLOSED-as-IC-lever (this memo); M7 row → pending-Norgate.
2. Ask-first landing item (operator): the Norgate trial signup (RS-3 r2/r3 trial-first;
   expected cost $0 trial / low monthly if converted — within the authorized data budget
   class but a NEW vendor account, hence ask-first).
3. VERDICTS.md: D3 row added (this memo), verification = the table in §2.
